// import { render, screen } from '@testing-library/react';
// import App from './App.js';

test('renders learn react link', () => {
  // console.log(<App />);
  // render(<App />);
  // const linkElement = screen.getByText(/parsely/i);
  // expect(linkElement).toBeInTheDocument();
});
